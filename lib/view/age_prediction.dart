import 'package:flutter/material.dart';

class AgePrediction extends StatelessWidget {
  const AgePrediction({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('나이 예측'),
      ),
    );
  }
}